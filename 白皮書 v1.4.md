白皮書 v1.4
目前實測結果
Galaxy A16 已成功連接。
App 已成功 build / deploy。
錄音權限已允許。
手動 REC / STOP 成功。
錄音檔可產生。
無 crash / 無 FATAL EXCEPTION。
快捷鍵 「按住音量上 + 音量下三下」可以啟動錄音。
問題：錄音中再次執行同一組快捷鍵，不會停止錄音。
目前快捷鍵只在 App 介面內有效，介面外無效；這符合 v1 前景 Activity 設計。
下一步優先順序
先修正快捷鍵 toggle：錄音中再觸發應停止錄音。
確認 VolumeKeyStateMachine 有每次 trigger 後 reset。
確認 UI / RecordingState 沒有阻擋 stop。
加 log：每次 hotkey trigger 都要印出目前狀態與動作。
介面外快捷鍵先列入 v2，不要現在混進來。
v2 再討論 Foreground Service / MediaSession / Accessibility 實驗模式。